fastapi>=0.115,<1.0
# Starlette 1.x is incompatible with current FastAPI releases (Router on_startup).
starlette>=0.37.2,<1.0
pydantic>=2.8,<3.0
uvicorn[standard]>=0.30,<1.0
# Jac graph engine for /v1/analysis-runs (subprocess worker). Optional if ALPHAWALKER_USE_JAC=0.
jaclang>=0.13.0
# Historical price data
yfinance>=0.2.36
